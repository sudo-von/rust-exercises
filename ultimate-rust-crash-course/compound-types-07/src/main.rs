fn main() {
    /* Tuple */
    let info: (u8, f64, f32) = (1, 1.0, 0.5); 
    // let jets = info.0;
    // let fuel = info.1;
    // let ammo = info.2;
    let (jets, fuel, ammod) = info;
    /* Arrays. */
    /* Value; How many. */
    let buf = [0; 3];
    let another_buf: [u8; 3] = [1,2,3];
    let first_buf = buf[0];
}
