fn main() {
    /* There are 6 types of strings in the std */
    let msg = "Hello";
    let msg = "abc".to_string();
    /* Unicode can be represented by 1,2,3 or 4 bytes. */
    let msg = String::from("abc");
}
